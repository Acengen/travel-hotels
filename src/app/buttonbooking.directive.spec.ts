import { ButtonbookingDirective } from './buttonbooking.directive';

describe('ButtonbookingDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonbookingDirective();
    expect(directive).toBeTruthy();
  });
});
